/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { VoiceMeasurementResponse } from '../../models/voice-measurement-response';
import { VoiceMeasurementTranscript } from '../../models/voice-measurement-transcript';

export interface ProcessVoiceMeasurementMeasurementsVoicePost$Params {
      body: VoiceMeasurementTranscript
}

export function processVoiceMeasurementMeasurementsVoicePost(http: HttpClient, rootUrl: string, params: ProcessVoiceMeasurementMeasurementsVoicePost$Params, context?: HttpContext): Observable<StrictHttpResponse<VoiceMeasurementResponse>> {
  const rb = new RequestBuilder(rootUrl, processVoiceMeasurementMeasurementsVoicePost.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<VoiceMeasurementResponse>;
    })
  );
}

processVoiceMeasurementMeasurementsVoicePost.PATH = '/measurements/voice';
