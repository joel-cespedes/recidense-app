/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PaginatedResponseMeasurementOut } from '../../models/paginated-response-measurement-out';

export interface GetMeasurementsByResidentMeasurementsResidentsResidentIdMeasurementsGet$Params {
  resident_id: string;

/**
 * Filter by residence ID
 */
  residence_id?: (string | null);

/**
 * Filter by measurement type: bp|spo2|weight|temperature
 */
  type?: (string | null);

/**
 * Time filter: 7d|15d|30d|1y|all
 */
  time_filter?: string;
  page?: number;
  size?: number;
  search?: (string | null);
  sort_by?: (string | null);
  sort_order?: ('asc' | 'desc' | null);
}

export function getMeasurementsByResidentMeasurementsResidentsResidentIdMeasurementsGet(http: HttpClient, rootUrl: string, params: GetMeasurementsByResidentMeasurementsResidentsResidentIdMeasurementsGet$Params, context?: HttpContext): Observable<StrictHttpResponse<PaginatedResponseMeasurementOut>> {
  const rb = new RequestBuilder(rootUrl, getMeasurementsByResidentMeasurementsResidentsResidentIdMeasurementsGet.PATH, 'get');
  if (params) {
    rb.path('resident_id', params.resident_id, {});
    rb.query('residence_id', params.residence_id, {});
    rb.query('type', params.type, {});
    rb.query('time_filter', params.time_filter, {});
    rb.query('page', params.page, {});
    rb.query('size', params.size, {});
    rb.query('search', params.search, {});
    rb.query('sort_by', params.sort_by, {});
    rb.query('sort_order', params.sort_order, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PaginatedResponseMeasurementOut>;
    })
  );
}

getMeasurementsByResidentMeasurementsResidentsResidentIdMeasurementsGet.PATH = '/measurements/residents/{resident_id}/measurements';
