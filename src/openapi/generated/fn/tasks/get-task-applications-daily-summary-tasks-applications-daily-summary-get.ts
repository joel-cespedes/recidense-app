/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PaginatedResponseTaskApplicationDailySummary } from '../../models/paginated-response-task-application-daily-summary';

export interface GetTaskApplicationsDailySummaryTasksApplicationsDailySummaryGet$Params {

/**
 * ID de la residencia
 */
  residence_id: string;

/**
 * Fecha inicio (YYYY-MM-DD)
 */
  date_from?: (string | null);

/**
 * Fecha fin (YYYY-MM-DD)
 */
  date_to?: (string | null);

/**
 * ID del profesional/gestor que asignó
 */
  assigned_by_id?: (string | null);
  search?: (string | null);
  page?: number;
  size?: number;
  sort_by?: (string | null);
  sort_order?: ('asc' | 'desc' | null);
}

export function getTaskApplicationsDailySummaryTasksApplicationsDailySummaryGet(http: HttpClient, rootUrl: string, params: GetTaskApplicationsDailySummaryTasksApplicationsDailySummaryGet$Params, context?: HttpContext): Observable<StrictHttpResponse<PaginatedResponseTaskApplicationDailySummary>> {
  const rb = new RequestBuilder(rootUrl, getTaskApplicationsDailySummaryTasksApplicationsDailySummaryGet.PATH, 'get');
  if (params) {
    rb.query('residence_id', params.residence_id, {});
    rb.query('date_from', params.date_from, {});
    rb.query('date_to', params.date_to, {});
    rb.query('assigned_by_id', params.assigned_by_id, {});
    rb.query('search', params.search, {});
    rb.query('page', params.page, {});
    rb.query('size', params.size, {});
    rb.query('sort_by', params.sort_by, {});
    rb.query('sort_order', params.sort_order, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PaginatedResponseTaskApplicationDailySummary>;
    })
  );
}

getTaskApplicationsDailySummaryTasksApplicationsDailySummaryGet.PATH = '/tasks/applications/daily-summary';
