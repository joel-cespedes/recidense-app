/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TaskApplicationResidentDay } from '../../models/task-application-resident-day';

export interface GetTaskApplicationsByResidentDateTasksApplicationsResidentResidentIdDateDateGet$Params {
  resident_id: string;
  date: string;

/**
 * ID del profesional/gestor que asignó las tareas
 */
  assigned_by_id?: (string | null);

/**
 * ID de la residencia
 */
  residence_id: string;
}

export function getTaskApplicationsByResidentDateTasksApplicationsResidentResidentIdDateDateGet(http: HttpClient, rootUrl: string, params: GetTaskApplicationsByResidentDateTasksApplicationsResidentResidentIdDateDateGet$Params, context?: HttpContext): Observable<StrictHttpResponse<TaskApplicationResidentDay>> {
  const rb = new RequestBuilder(rootUrl, getTaskApplicationsByResidentDateTasksApplicationsResidentResidentIdDateDateGet.PATH, 'get');
  if (params) {
    rb.path('resident_id', params.resident_id, {});
    rb.path('date', params.date, {});
    rb.query('assigned_by_id', params.assigned_by_id, {});
    rb.query('residence_id', params.residence_id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<TaskApplicationResidentDay>;
    })
  );
}

getTaskApplicationsByResidentDateTasksApplicationsResidentResidentIdDateDateGet.PATH = '/tasks/applications/resident/{resident_id}/date/{date}';
